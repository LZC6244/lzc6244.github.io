- 文件名统一采用 `YYYY-MM-DD-中文标题.md` 格式，确保 Jekyll 按时间排序生成 URL。
- 每篇文档顶部使用 YAML Front Matter 固定字段 `layout`、`title`、`create_time`、`update_time`、`categories`，其中 `layout` 固定为 `post`。
- 图片资源通过绝对路径 `/imgs/<主题>/<年>/<序号>_<描述>.png` 引用，与文章年份目录保持一致。
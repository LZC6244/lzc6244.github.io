纯静态内容目录，遵循 Jekyll `_posts` 约定：子目录为年份（2019–2026），每个文件以 `YYYY-MM-DD-标题.md` 命名，由 Jekyll 自动解析为站点页面；每篇文章头部使用 YAML Front Matter 声明 `layout: post`、`title`、`create_time`、`update_time`、`categories` 等元数据，正文采用 Markdown 编写并引用 `/imgs/...` 下的图片资源。模块本身不含任何构建或路由逻辑，仅作为 Jekyll 的输入源。
- Jekyll 数据源文件统一以 `---\nlayout: null\n---` 的 YAML Front Matter 开头，禁用默认布局直接输出原始格式（XML/JSON）。
- Jekyll 变量输出一律经 `xml_escape` 或 `jsonify` 转义后再拼入模板，避免注入风险。
- URL 拼接统一采用 `| prepend: site.baseurl | prepend: site.url` 链式过滤器，保证部署到子路径时链接正确。